import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export function MeterArea(){
    return (<Card>
        <CardHeader>
            <CardTitle>Change Meter Area</CardTitle>
        </CardHeader>
    </Card>);
}